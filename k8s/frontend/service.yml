apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: cloudforge-dev
  labels:
    app: frontend
    environment: dev
    project: cloudforge

spec:
  type: ClusterIP
  selector:
    app: frontend

  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP